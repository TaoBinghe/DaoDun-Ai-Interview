spring:
  application:
    name: backend

  # 数据库配置（PostgreSQL）
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_interview
    username: postgres
    password: 20041230Tbh
    driver-class-name: org.postgresql.Driver

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  # 邮件配置（QQ 邮箱）
  mail:
    host: smtp.qq.com
    port: 587
    username: 1741893518@qq.com
    password: upfdortpsdanejha
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

# JWT 配置
jwt:
  secret: v9y$B&E)H@McQfTjWnZr4u7x!A%D*G-K
  # Access Token 有效期（秒）：2小时
  access-token-expiration: 7200
  # Refresh Token 有效期（秒）：7天
  refresh-token-expiration: 604800
  # 记住我时 Refresh Token 有效期（秒）：14天
  refresh-token-remember-me-expiration: 1209600

# 服务端口
server:
  port: 8081
  servlet:
    context-path: /
