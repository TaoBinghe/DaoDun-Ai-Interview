# AI模拟面试与能力提升平台 - 接口文档 V1.0

## 1. 用户认证相关接口

### 1.1 发送注册验证码

#### 1.1.1 基本信息

> 请求路径：/api/auth/send-register-code
>
> 请求方式：POST
>
> 接口描述：向指定邮箱发送注册用验证码，验证码 5 分钟内有效；同一邮箱 60 秒内只能请求一次；若邮箱已注册则提示直接登录。

#### 1.1.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称 | 说明 | 类型   | 是否必须 | 备注                     |
| -------- | ---- | ------ | -------- | ------------------------ |
| email    | 邮箱 | string | 是       | 符合标准电子邮箱格式     |

请求数据样例：

```json
{
    "email": "user@example.com"
}
```

#### 1.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称 | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ---- | ------ | -------- | ------ | ---------------------------- | -------- |
| code | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg  | string | 非必须   |        | 提示信息                     |          |
| data | object | 非必须   |        | 返回的数据，本接口为 null    |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "验证码已发送，请查收邮件",
    "data": null
}
```

#### 1.1.4 备注说明

> 若该邮箱已注册，接口返回 code 为 400，msg 为「该邮箱已注册，请直接登录」。
> 若 60 秒内重复请求，返回「发送过于频繁，请 60 秒后再试」。



### 1.2 发送登录验证码

#### 1.2.1 基本信息

> 请求路径：/api/auth/send-login-code
>
> 请求方式：POST
>
> 接口描述：向已注册邮箱发送登录用验证码，验证码 5 分钟内有效；同一邮箱 60 秒内只能请求一次；若邮箱未注册则提示未注册。

#### 1.2.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称 | 说明 | 类型   | 是否必须 | 备注                 |
| -------- | ---- | ------ | -------- | -------------------- |
| email    | 邮箱 | string | 是       | 符合标准电子邮箱格式 |

请求数据样例：

```json
{
    "email": "user@example.com"
}
```

#### 1.2.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称 | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ---- | ------ | -------- | ------ | ---------------------------- | -------- |
| code | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg  | string | 非必须   |        | 提示信息                     |          |
| data | object | 非必须   |        | 返回的数据，本接口为 null    |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "验证码已发送，请查收邮件",
    "data": null
}
```

#### 1.2.4 备注说明

> 若该邮箱未注册，接口返回 code 为 400，msg 为「该邮箱未注册」。



### 1.3 用户注册

#### 1.3.1 基本信息

> 请求路径：/api/auth/register
>
> 请求方式：POST
>
> 接口描述：使用邮箱、验证码、用户名、密码完成注册；密码使用 BCrypt 加密存储；校验验证码、邮箱与用户名的唯一性。

#### 1.3.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称 | 说明     | 类型   | 是否必须 | 备注                                           |
| -------- | -------- | ------ | -------- | ---------------------------------------------- |
| email    | 邮箱     | string | 是       | 符合标准电子邮箱格式，且未被注册               |
| code     | 验证码   | string | 是       | 6 位纯数字，须为发送注册验证码接口下发的验证码 |
| username | 用户名   | string | 是       | 5~10 个字符，支持中英文、数字，全局唯一        |
| password | 密码     | string | 是       | 5~16 位，须包含英文大小写字母和数字的组合     |

请求数据样例：

```json
{
    "email": "user@example.com",
    "code": "123456",
    "username": "张三abc",
    "password": "Abc12345"
}
```

#### 1.3.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称 | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ---- | ------ | -------- | ------ | ---------------------------- | -------- |
| code | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg  | string | 非必须   |        | 提示信息                     |          |
| data | object | 非必须   |        | 返回的数据，本接口为 null    |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "注册成功",
    "data": null
}
```

#### 1.3.4 备注说明

> 验证码错误或过期时返回「验证码已过期，请重新获取」或「验证码错误」。
> 用户名已存在时返回「该用户名已被占用，请尝试其他名称」。
> 邮箱已注册时返回「该邮箱已注册，请直接登录」。



### 1.4 账号密码登录

#### 1.4.1 基本信息

> 请求路径：/api/auth/login/password
>
> 请求方式：POST
>
> 接口描述：使用用户名和密码登录；成功后返回 Access Token、Refresh Token 及用户基本信息；rememberMe 为 true 时 Refresh Token 有效期为 14 天，否则为 7 天。

#### 1.4.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称   | 说明     | 类型    | 是否必须 | 备注                                       |
| ---------- | -------- | ------- | -------- | ------------------------------------------ |
| username   | 用户名   | string  | 是       | 已注册的用户名                             |
| password   | 密码     | string  | 是       | 明文密码                                   |
| rememberMe | 记住我   | boolean | 否       | 默认 false；为 true 时延长 Refresh Token 有效期 |

请求数据样例：

```json
{
    "username": "张三abc",
    "password": "Abc12345",
    "rememberMe": false
}
```

#### 1.4.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称   | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------ | ------ | -------- | ------ | ---------------------------- | -------- |
| code   | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg    | string | 非必须   |        | 提示信息                     |          |
| data   | object | 必须     |        | 登录结果                     |          |
| \|-accessToken  | string | 非必须 | | 访问令牌，用于请求头 Authorization | |
| \|-refreshToken | string | 非必须 | | 刷新令牌，用于换取新的 accessToken | |
| \|-userId      | number | 非必须 | | 用户主键 ID                  |          |
| \|-username    | string | 非必须 | | 用户名                       |          |
| \|-email       | string | 非必须 | | 邮箱                         |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "登录成功",
    "data": {
        "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "userId": 1,
        "username": "张三abc",
        "email": "user@example.com"
    }
}
```

#### 1.4.4 备注说明

> 用户名或密码错误时，统一返回 code 400，msg「用户名或密码错误」（不区分具体错误项）。
> 登录成功后，后续请求需在请求头中携带：Authorization: Bearer \<accessToken\>。Access Token 有效期 2 小时；过期后可使用 refreshToken 换取新的 accessToken（刷新接口若已实现则在此补充路径）。



### 1.5 邮箱验证码登录

#### 1.5.1 基本信息

> 请求路径：/api/auth/login/email
>
> 请求方式：POST
>
> 接口描述：使用已注册邮箱和登录验证码登录；成功后返回 Access Token、Refresh Token 及用户基本信息；rememberMe 为 true 时 Refresh Token 有效期为 14 天，否则为 7 天。

#### 1.5.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称   | 说明     | 类型    | 是否必须 | 备注                                           |
| ---------- | -------- | ------- | -------- | ---------------------------------------------- |
| email      | 邮箱     | string  | 是       | 已注册邮箱，符合标准电子邮箱格式               |
| code       | 验证码   | string  | 是       | 6 位纯数字，须为发送登录验证码接口下发的验证码 |
| rememberMe | 记住我   | boolean | 否       | 默认 false；为 true 时延长 Refresh Token 有效期 |

请求数据样例：

```json
{
    "email": "user@example.com",
    "code": "123456",
    "rememberMe": true
}
```

#### 1.5.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称   | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------ | ------ | -------- | ------ | ---------------------------- | -------- |
| code   | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg    | string | 非必须   |        | 提示信息                     |          |
| data   | object | 必须     |        | 登录结果                     |          |
| \|-accessToken  | string | 非必须 | | 访问令牌，用于请求头 Authorization | |
| \|-refreshToken | string | 非必须 | | 刷新令牌，用于换取新的 accessToken | |
| \|-userId      | number | 非必须 | | 用户主键 ID                  |          |
| \|-username    | string | 非必须 | | 用户名                       |          |
| \|-email       | string | 非必须 | | 邮箱                         |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "登录成功",
    "data": {
        "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "userId": 1,
        "username": "张三abc",
        "email": "user@example.com"
    }
}
```

#### 1.5.4 备注说明

> 邮箱未注册时返回 code 400，msg「该邮箱未注册」。
> 验证码错误或过期时返回「验证码错误」或「验证码已过期，请重新获取」。
> 除 /api/auth/** 外的接口，若未携带有效 Token 或 Token 过期，HTTP 状态码为 401，响应体为 JSON，如：{"code":401,"msg":"未登录或 Token 已过期，请重新登录"}。



### 1.6 刷新 Token

#### 1.6.1 基本信息

> 请求路径：/api/auth/refresh
>
> 请求方式：POST
>
> 接口描述：使用 Refresh Token 换取新的 Access Token；成功后返回新的 accessToken、refreshToken 及用户基本信息。

#### 1.6.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称      | 说明     | 类型   | 是否必须 | 备注                     |
| ------------- | -------- | ------ | -------- | ------------------------ |
| refreshToken  | 刷新令牌 | string | 是       | 登录时下发的 refreshToken |

请求数据样例：

```json
{
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### 1.6.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称   | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------ | ------ | -------- | ------ | ---------------------------- | -------- |
| code   | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg    | string | 非必须   |        | 提示信息                     |          |
| data   | object | 必须     |        | 登录结果                     |          |
| \|-accessToken  | string | 非必须 | | 新的访问令牌                 |          |
| \|-refreshToken | string | 非必须 | | 新的刷新令牌                 |          |
| \|-userId      | number | 非必须 | | 用户主键 ID                  |          |
| \|-username    | string | 非必须 | | 用户名                       |          |
| \|-email       | string | 非必须 | | 邮箱                         |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "刷新成功",
    "data": {
        "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "userId": 1,
        "username": "张三abc",
        "email": "user@example.com"
    }
}
```

#### 1.6.4 备注说明

> Refresh Token 无效或过期时返回 code 400，msg 为相应错误提示。前端需引导用户重新登录。

---

## 2. 用户相关接口

### 2.1 获取当前用户信息

#### 2.1.1 基本信息

> 请求路径：/api/user/me
>
> 请求方式：GET
>
> 接口描述：获取当前已登录用户的基本信息；需携带有效 Access Token。

#### 2.1.2 请求参数

无

#### 2.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称       | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ---------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code       | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg        | string | 非必须   |        | 提示信息                     |          |
| data       | object | 必须     |        | 用户信息                     |          |
| \|-userId   | number | 非必须   |        | 用户主键 ID                  |          |
| \|-username | string | 非必须   |        | 用户名                       |          |
| \|-email    | string | 非必须   |        | 邮箱                         |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "userId": 1,
        "username": "张三abc",
        "email": "user@example.com"
    }
}
```

---

## 3. 岗位相关接口

### 3.1 岗位列表

#### 3.1.1 基本信息

> 请求路径：/api/position/list
>
> 请求方式：GET
>
> 接口描述：获取全部岗位列表，用于创建面试时选择岗位。

#### 3.1.2 请求参数

无

#### 3.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称     | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| -------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code     | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg      | string | 非必须   |        | 提示信息                     |          |
| data     | array  | 必须     |        | 岗位列表                     |          |
| \|-id     | number | 非必须   |        | 岗位主键 ID                  |          |
| \|-name   | string | 非必须   |        | 岗位名称                     |          |
| \|-description | string | 非必须 | | 岗位描述                     |          |
| \|-sortOrder | number | 非必须 | | 排序序号                     |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "id": 1,
            "name": "Java后端开发",
            "description": "Java 后端开发岗位",
            "sortOrder": 1
        }
    ]
}
```

---

## 4. 题目相关接口

### 4.1 按岗位查询题目列表

#### 4.1.1 基本信息

> 请求路径：/api/question/list
>
> 请求方式：GET
>
> 接口描述：按岗位 ID 查询该岗位下的全部题目列表，用于管理或调试。

#### 4.1.2 请求参数

请求参数格式：queryString

请求参数说明：

| 参数名称    | 说明     | 类型   | 是否必须 | 备注     |
| ----------- | -------- | ------ | -------- | -------- |
| positionId  | 岗位 ID  | number | 是       | 主键 ID  |

请求数据样例：

```shell
positionId=1
```

#### 4.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称         | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------------ | ------ | -------- | ------ | ---------------------------- | -------- |
| code         | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg          | string | 非必须   |        | 提示信息                     |          |
| data         | array  | 必须     |        | 题目列表                     |          |
| \|-id        | number | 非必须   |        | 题目主键 ID                  |          |
| \|-positionId| number | 非必须   |        | 岗位 ID                      |          |
| \|-content   | string | 非必须   |        | 题目内容                     |          |
| \|-type      | string | 非必须   |        | 题型：TECHNICAL/PROJECT/SCENARIO/BEHAVIOR |          |
| \|-difficulty | number | 非必须   |        | 难度 1~5                     |          |
| \|-sortOrder | number | 非必须   |        | 排序序号                     |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "id": 1,
            "positionId": 1,
            "content": "请介绍 Spring 的 IoC 与 AOP",
            "type": "TECHNICAL",
            "difficulty": 2,
            "sortOrder": 1
        }
    ]
}
```

### 4.2 随机抽题

#### 4.2.1 基本信息

> 请求路径：/api/question/draw
>
> 请求方式：GET
>
> 接口描述：按岗位、题型、难度随机抽取题目，供面试流程使用；type、difficulty 不传则不限；count 默认 1。

#### 4.2.2 请求参数

请求参数格式：queryString

请求参数说明：

| 参数名称   | 说明     | 类型   | 是否必须 | 备注                                      |
| ---------- | -------- | ------ | -------- | ----------------------------------------- |
| positionId | 岗位 ID  | number | 是       | 主键 ID                                   |
| type       | 题型     | string | 否       | TECHNICAL/PROJECT/SCENARIO/BEHAVIOR，不传则不限 |
| difficulty | 难度     | number | 否       | 1~5，不传则不限难度                       |
| count      | 抽取数量 | number | 否       | 默认 1                                    |

请求数据样例：

```shell
positionId=1&type=TECHNICAL&difficulty=2&count=1
```

#### 4.2.3 响应数据

响应数据类型：application/json

响应参数说明：同 4.1.3（data 为题目列表 array）。

响应数据样例：同 4.1.3。

---

## 5. 面试会话相关接口

> 以下接口均需在请求头携带：**Authorization: Bearer \<accessToken\>**。

### 5.1 创建面试会话

#### 5.1.1 基本信息

> 请求路径：/api/interview/sessions
>
> 请求方式：POST
>
> 接口描述：创建一场新面试会话；可绑定简历、限定首题题型；成功后返回会话详情。

#### 5.1.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称   | 说明       | 类型   | 是否必须 | 备注                                   |
| ---------- | ---------- | ------ | -------- | -------------------------------------- |
| positionId | 岗位 ID    | number | 是       | 主键 ID                                |
| resumeId   | 简历 ID    | number | 否       | 已上传简历 ID，绑定后 AI 可个性化提问   |
| type       | 首题题型   | string | 否       | TECHNICAL/PROJECT/SCENARIO/BEHAVIOR，不传则不限 |

请求数据样例：

```json
{
    "positionId": 1,
    "resumeId": 1,
    "type": "TECHNICAL"
}
```

#### 5.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称            | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| --------------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code            | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg             | string | 非必须   |        | 提示信息                     |          |
| data            | object | 必须     |        | 会话详情                     |          |
| \|-sessionId    | number | 非必须   |        | 会话主键 ID                  |          |
| \|-positionId   | number | 非必须   |        | 岗位 ID                      |          |
| \|-positionName | string | 非必须   |        | 岗位名称                     |          |
| \|-status       | string | 非必须   |        | 状态：IN_PROGRESS/COMPLETED 等 |          |
| \|-currentTurnIndex | number | 非必须 | | 当前轮次下标（从 0 起）      |          |
| \|-hasResume    | boolean| 非必须   |        | 是否绑定简历                 |          |
| \|-resumeFileName | string | 非必须 | | 简历文件名                   |          |
| \|-startedAt    | string | 非必须   |        | 开始时间，ISO 格式           |          |
| \|-endedAt      | string | 非必须   |        | 结束时间                     |          |
| \|-createTime   | string | 非必须   |        | 创建时间                     |          |
| \|-turns        | array  | 非必须   |        | 对话轮次列表，见下方 Turn 结构 |        |

Turn 结构（turns 数组元素）：

| 名称        | 类型   | 说明                           |
| ----------- | ------ | ------------------------------ |
| id          | number | 轮次主键 ID                    |
| turnIndex   | number | 轮次序号                       |
| role        | string | INTERVIEWER / USER             |
| messageType | string | QUESTION / FOLLOW_UP / ANSWER  |
| content     | string | 文本内容                       |
| questionId  | number | 关联题目 ID（可为 null）       |
| latencyMs   | number | 响应耗时毫秒（可为 null）      |
| createTime  | string | 创建时间                       |

响应数据样例：

```json
{
    "code": 200,
    "msg": "面试会话创建成功",
    "data": {
        "sessionId": 1,
        "positionId": 1,
        "positionName": "Java后端开发",
        "status": "IN_PROGRESS",
        "currentTurnIndex": 0,
        "hasResume": true,
        "resumeFileName": "resume.pdf",
        "startedAt": "2025-03-17T10:00:00",
        "endedAt": null,
        "createTime": "2025-03-17T10:00:00",
        "turns": []
    }
}
```

### 5.2 获取会话详情

#### 5.2.1 基本信息

> 请求路径：/api/interview/sessions/{id}
>
> 请求方式：GET
>
> 接口描述：根据会话 ID 获取会话详情，包含全部对话轮次（turns）。

#### 5.2.2 请求参数

请求参数格式：path

| 参数名称 | 说明   | 类型   | 是否必须 | 备注      |
| -------- | ------ | ------ | -------- | --------- |
| id       | 会话 ID| number | 是       | 路径参数  |

#### 5.2.3 响应数据

响应数据类型：application/json；响应结构同 5.1.3，data 为会话详情（含 turns 列表）。

### 5.3 提交回答（一轮对话）

#### 5.3.1 基本信息

> 请求路径：/api/interview/sessions/{id}/turns
>
> 请求方式：POST
>
> 接口描述：提交用户回答内容，后端记录用户轮次并调用 AI 生成面试官下一句（追问或下一题），返回用户轮与面试官轮及当前轮次下标。

#### 5.3.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称     | 说明     | 类型   | 是否必须 | 备注                           |
| ------------ | -------- | ------ | -------- | ------------------------------ |
| content      | 回答内容 | string | 是       | 不能为空，最多 2000 字         |
| clientTurnId | 幂等键   | string | 否       | 前端生成（如 UUID），防重复提交 |

请求数据样例：

```json
{
    "content": "我的回答内容...",
    "clientTurnId": "uuid-xxx-xxx"
}
```

#### 5.3.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称              | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ----------------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code              | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg               | string | 非必须   |        | 提示信息                     |          |
| data              | object | 必须     |        | 本轮结果                     |          |
| \|-userTurn       | object | 非必须   |        | 用户轮次，结构同 Turn        |          |
| \|-interviewerTurn| object | 非必须   |        | 面试官轮次，结构同 Turn      |          |
| \|-currentTurnIndex | number | 非必须 | | 当前轮次下标                 |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "userTurn": { "id": 1, "turnIndex": 1, "role": "USER", "messageType": "ANSWER", "content": "我的回答...", "questionId": null, "latencyMs": null, "createTime": "2025-03-17T10:01:00" },
        "interviewerTurn": { "id": 2, "turnIndex": 2, "role": "INTERVIEWER", "messageType": "FOLLOW_UP", "content": "请再详细说说...", "questionId": 1, "latencyMs": 1200, "createTime": "2025-03-17T10:01:02" },
        "currentTurnIndex": 2
    }
}
```

### 5.4 结束面试会话

#### 5.4.1 基本信息

> 请求路径：/api/interview/sessions/{id}/complete
>
> 请求方式：PATCH
>
> 接口描述：将会话状态置为已结束（COMPLETED），幂等；可携带情绪时间线，用于异步生成评估报告。

#### 5.4.2 请求参数

请求参数格式：application/json（可选，可不传或传空对象）

请求参数说明：

| 参数名称       | 说明         | 类型  | 是否必须 | 备注                                   |
| -------------- | ------------ | ----- | -------- | -------------------------------------- |
| emotionTimeline | 情绪时间线   | array | 否       | 元素见下表，可为空数组                 |
| \|-timestamp   | 相对开始的毫秒数 | number | 非必须 | 面试开始后经过的毫秒数                 |
| \|-emotion     | 情绪标签     | string | 非必须 | 如 neutral/happy/angry                 |
| \|-confidence  | 置信度       | number | 非必须 | 0.0~1.0                                |

请求数据样例：

```json
{
    "emotionTimeline": [
        { "timestamp": 0, "emotion": "neutral", "confidence": 0.9 },
        { "timestamp": 60000, "emotion": "happy", "confidence": 0.8 }
    ]
}
```

#### 5.4.3 响应数据

响应数据类型：application/json

| 名称 | 类型   | 是否必须 | 备注                         |
| ---- | ------ | -------- | ---------------------------- |
| code | number | 必须     | 200-成功，非200-失败         |
| msg  | string | 非必须   | 提示信息                     |
| data | object | 非必须   | 本接口为 null                |

响应数据样例：

```json
{
    "code": 200,
    "msg": "面试已结束",
    "data": null
}
```

### 5.5 获取面试评估报告

#### 5.5.1 基本信息

> 请求路径：/api/interview/sessions/{id}/evaluation
>
> 请求方式：GET
>
> 接口描述：获取该会话的评估报告；status 为 GENERATING 时表示生成中，前端可轮询（如每 3 秒）；status 为 READY 时 report 有值。

#### 5.5.2 请求参数

请求参数格式：path；路径参数 id 为会话 ID。

#### 5.5.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称           | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| -------------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code           | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg            | string | 非必须   |        | 提示信息                     |          |
| data           | object | 必须     |        | 评估报告                     |          |
| \|-sessionId   | number | 非必须   |        | 会话 ID                      |          |
| \|-status      | string | 非必须   |        | GENERATING/READY/FAILED/NOT_STARTED/INSUFFICIENT_DATA |          |
| \|-message     | string | 非必须   |        | 状态说明（如失败原因）       |          |
| \|-report      | object | 非必须   |        | 仅 status=READY 时非 null   |          |
| \|-overallScore| number | 非必须   |        | 综合得分                     |          |
| \|-overallComment| string | 非必须 | | 综合评语                     |          |
| \|-knowledgeAssessment | object | 非必须 | | 知识评估（strengths/weaknesses/topicDetails） |          |
| \|-emotionAssessment | object | 非必须 | | 情绪评估（summary/positives/issues） |          |
| \|-recommendations | object | 非必须 | | 改进建议（learning/emotional） |          |

响应数据样例（status=READY）：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "sessionId": 1,
        "status": "READY",
        "message": null,
        "report": {
            "overallScore": 75,
            "overallComment": "整体表现良好...",
            "knowledgeAssessment": { "strengths": [], "weaknesses": [], "topicDetails": [] },
            "emotionAssessment": { "summary": "", "positives": [], "issues": [] },
            "recommendations": { "learning": [], "emotional": [] }
        }
    }
}
```

### 5.6 面试记录列表

#### 5.6.1 基本信息

> 请求路径：/api/interview/sessions
>
> 请求方式：GET
>
> 接口描述：查询当前用户的历史面试会话列表，按开始时间倒序。

#### 5.6.2 请求参数

无

#### 5.6.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称     | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| -------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code     | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg      | string | 非必须   |        | 提示信息                     |          |
| data     | array  | 必须     |        | 会话摘要列表                 |          |
| \|-sessionId | number | 非必须 | | 会话 ID                      |          |
| \|-positionId | number | 非必须 | | 岗位 ID                      |          |
| \|-positionName | string | 非必须 | | 岗位名称                     |          |
| \|-status | string | 非必须   |        | 状态                         |          |
| \|-currentTurnIndex | number | 非必须 | | 当前轮次下标                 |          |
| \|-startedAt | string | 非必须 | | 开始时间                     |          |
| \|-endedAt | string | 非必须 | | 结束时间                     |          |
| \|-createTime | string | 非必须 | | 创建时间                     |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "sessionId": 1,
            "positionId": 1,
            "positionName": "Java后端开发",
            "status": "COMPLETED",
            "currentTurnIndex": 10,
            "startedAt": "2025-03-17T10:00:00",
            "endedAt": "2025-03-17T10:30:00",
            "createTime": "2025-03-17T10:00:00"
        }
    ]
}
```

---

## 6. 简历相关接口

> 以下接口均需在请求头携带：**Authorization: Bearer \<accessToken\>**。

### 6.1 上传简历

#### 6.1.1 基本信息

> 请求路径：/api/resume/upload
>
> 请求方式：POST
>
> 接口描述：上传简历文件并解析，返回简历 ID 及解析结果（项目、技能、教育等）；创建面试时可绑定 resumeId。

#### 6.1.2 请求参数

请求参数格式：multipart/form-data

请求参数说明：

| 参数名称 | 说明     | 类型 | 是否必须 | 备注           |
| -------- | -------- | ---- | -------- | -------------- |
| file     | 简历文件 | file | 是       | 如 PDF、Word 等 |

请求数据样例：无

#### 6.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称          | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------------- | ------ | -------- | ------ | ---------------------------- | -------- |
| code          | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg           | string | 非必须   |        | 提示信息                     |          |
| data          | object | 必须     |        | 上传结果                     |          |
| \|-resumeId   | number | 非必须   |        | 简历主键 ID                  |          |
| \|-fileName   | string | 非必须   |        | 文件名                       |          |
| \|-charCount  | number | 非必须   |        | 字符数                       |          |
| \|-pageCount  | number | 非必须   |        | 页数                         |          |
| \|-previewText | string | 非必须  |        | 预览摘要文本                 |          |
| \|-projects   | array  | 非必须   |        | 项目名称列表                 |          |
| \|-skills     | array  | 非必须   |        | 技能列表                     |          |
| \|-education  | array  | 非必须   |        | 教育经历列表                 |          |
| \|-projectDetails  | array | 非必须 | | 项目详情（name/timeRange/role/techStack/description） |          |
| \|-skillDetails    | array | 非必须 | | 技能详情（category/items）   |          |
| \|-educationDetails| array | 非必须 | | 教育详情（school/degree/major/timeRange/description） |          |
| \|-uploadedAt  | string | 非必须 | | 上传时间                     |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": "简历上传并解析成功",
    "data": {
        "resumeId": 1,
        "fileName": "resume.pdf",
        "charCount": 3000,
        "pageCount": 2,
        "previewText": "摘要...",
        "projects": ["项目A"],
        "skills": ["Java"],
        "education": ["XX大学"],
        "projectDetails": [],
        "skillDetails": [],
        "educationDetails": [],
        "uploadedAt": "2025-03-17T09:00:00"
    }
}
```

### 6.2 我的简历列表

#### 6.2.1 基本信息

> 请求路径：/api/resume/me
>
> 请求方式：GET
>
> 接口描述：获取当前用户上传的全部简历列表（结构同单条上传返回的 data）。

#### 6.2.2 请求参数

无

#### 6.2.3 响应数据

响应数据类型：application/json；data 为 array，元素结构同 6.1.3 的 data。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "resumeId": 1,
            "fileName": "resume.pdf",
            "charCount": 3000,
            "pageCount": 2,
            "previewText": "摘要...",
            "projects": [],
            "skills": [],
            "education": [],
            "projectDetails": [],
            "skillDetails": [],
            "educationDetails": [],
            "uploadedAt": "2025-03-17T09:00:00"
        }
    ]
}
```

### 6.3 删除简历

#### 6.3.1 基本信息

> 请求路径：/api/resume/{id}
>
> 请求方式：DELETE
>
> 接口描述：根据简历 ID 删除当前用户的某份简历；id 为路径参数。

#### 6.3.2 请求参数

请求参数格式：path

| 参数名称 | 说明   | 类型   | 是否必须 | 备注     |
| -------- | ------ | ------ | -------- | -------- |
| id       | 简历 ID| number | 是       | 路径参数 |

#### 6.3.3 响应数据

响应数据类型：application/json

响应数据样例：

```json
{
    "code": 200,
    "msg": "简历删除成功",
    "data": null
}
```

---

## 7. 知识库相关接口

### 7.1 重建知识库

#### 7.1.1 基本信息

> 请求路径：/api/knowledge/rebuild
>
> 请求方式：POST
>
> 接口描述：重新从数据源拉取并入库全部知识块，并生成向量；返回本次入库的 chunk 数量。一般用于管理/运维。

#### 7.1.2 请求参数

无

#### 7.1.3 响应数据

响应数据类型：application/json

响应参数说明：

| 名称   | 类型   | 是否必须 | 默认值 | 备注                         | 其他信息 |
| ------ | ------ | -------- | ------ | ---------------------------- | -------- |
| code   | number | 必须     |        | 响应码，200-成功，非200-失败 |          |
| msg    | string | 非必须   |        | 提示信息                     |          |
| data   | object | 必须     |        | ingestedChunks、message     |          |
| \|-ingestedChunks | number | 非必须 | | 本次入库的 chunk 数量       |          |
| \|-message | string | 非必须 | | 提示信息                     |          |

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "ingestedChunks": 100,
        "message": "知识库重建完成"
    }
}
```

### 7.2 清空向量

#### 7.2.1 基本信息

> 请求路径：/api/knowledge/clear-embeddings
>
> 请求方式：POST
>
> 接口描述：清空所有 chunk 的向量，便于后续重新生成；调用后需再调 rebuild 或重新入库才会生成新向量。

#### 7.2.2 请求参数

无

#### 7.2.3 响应数据

响应数据类型：application/json；data 含 clearedCount、message。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "clearedCount": 100,
        "message": "已清空全部向量"
    }
}
```

### 7.3 知识库统计

#### 7.3.1 基本信息

> 请求路径：/api/knowledge/stats
>
> 请求方式：GET
>
> 接口描述：获取知识库统计信息：总 chunk 数、已生成向量的数量、未生成向量的数量。

#### 7.3.2 请求参数

无

#### 7.3.3 响应数据

响应数据类型：application/json

响应参数说明：data 含 totalChunks、withEmbedding、withoutEmbedding（number）。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "totalChunks": 100,
        "withEmbedding": 98,
        "withoutEmbedding": 2
    }
}
```

### 7.4 知识库检索验证

#### 7.4.1 基本信息

> 请求路径：/api/knowledge/verify
>
> 请求方式：POST
>
> 接口描述：使用与面试相同的混合检索逻辑，根据岗位、查询文本、关键词检索，返回命中的 chunk 列表，用于核对 RAG 效果。

#### 7.4.2 请求参数

请求参数格式：application/json

请求参数说明：

| 参数名称     | 说明       | 类型   | 是否必须 | 备注                           |
| ------------ | ---------- | ------ | -------- | ------------------------------ |
| positionName | 岗位名称   | string | 是       | 与知识库 position_name 一致    |
| queryText    | 检索文本   | string | 否       | 模拟问题+回答拼接              |
| keywords     | 关键词列表 | array  | 否       | 字符串数组                     |

请求数据样例：

```json
{
    "positionName": "Java后端开发",
    "queryText": "Spring IoC",
    "keywords": ["Spring"]
}
```

#### 7.4.3 响应数据

响应数据类型：application/json；data 为 array，元素包含 id、documentId、title、categoryLevel1、categoryLevel2、keywords、difficulty、canonicalQuestion、answerKeyPoints、exampleAnswer、followUps、scoringPoints、pitfalls、sourceOrder 等字段。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "id": 1,
            "documentId": 1,
            "title": "Spring IoC",
            "categoryLevel1": "框架",
            "categoryLevel2": "Spring",
            "keywords": "IoC,DI",
            "difficulty": 2,
            "canonicalQuestion": "请介绍 Spring IoC",
            "answerKeyPoints": "...",
            "exampleAnswer": "...",
            "followUps": "...",
            "scoringPoints": "...",
            "pitfalls": "...",
            "sourceOrder": 1
        }
    ]
}
```

---

## 8. 语音诊断相关接口

### 8.1 TTS 诊断

#### 8.1.1 基本信息

> 请求路径：/api/voice/diagnostics/tts
>
> 请求方式：POST
>
> 接口描述：使用当前配置的 TTS 合成指定文本，返回是否成功、HTTP 状态、音频长度、错误信息等，用于排查 TTS 配置问题。

#### 8.1.2 请求参数

请求参数格式：application/json（可选，可不传）

请求参数说明：

| 参数名称 | 说明     | 类型   | 是否必须 | 备注     |
| -------- | -------- | ------ | -------- | -------- |
| text     | 合成文本 | string | 否       | 不传则使用默认文本 |

请求数据样例：

```json
{
    "text": "你好，这是一段测试语音"
}
```

#### 8.1.3 响应数据

响应数据类型：application/json

响应参数说明：data 含 config、httpStatus、success、audioBase64Length、mimeType、responseSnippet、errorMessage 等。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "config": {},
        "httpStatus": 200,
        "success": true,
        "audioBase64Length": 12345,
        "mimeType": "audio/wav",
        "responseSnippet": null,
        "errorMessage": null
    }
}
```

### 8.2 实时语音端到端诊断

#### 8.2.1 基本信息

> 请求路径：/api/voice/diagnostics/realtime-voice
>
> 请求方式：POST
>
> 接口描述：使用当前配置的 STT + TTS 做一次端到端合成，返回是否成功、使用的 speaker、音频长度、错误信息等，便于排查「speaker id invalid」等问题。

#### 8.2.2 请求参数

同 8.1.2（可选 body，含 text）。

#### 8.2.3 响应数据

响应数据类型：application/json；data 含 config、success、speakerUsed、audioBytesLength、errorMessage。

响应数据样例：

```json
{
    "code": 200,
    "msg": null,
    "data": {
        "config": {},
        "success": true,
        "speakerUsed": "default",
        "audioBytesLength": 32000,
        "errorMessage": null
    }
}
```

---

## 9. 通用说明

### 9.1 基础路径

> 如无特殊说明，接口基础路径为：`http://localhost:8080`（以实际部署为准）。

### 9.2 统一响应结构

| 名称 | 类型   | 说明                         |
| ---- | ------ | ---------------------------- |
| code | number | 200 表示成功，400/401/403/500 等表示失败 |
| msg  | string | 提示信息                     |
| data | object | 业务数据，失败时多为 null    |

### 9.3 认证方式

> 需要登录的接口，请在请求头中携带：  
> **Authorization: Bearer \<accessToken\>**  
> 其中 accessToken 为登录接口返回的 data.accessToken。

### 9.4 错误码说明

| code | 说明                     |
| ---- | ------------------------ |
| 200  | 成功                     |
| 400  | 请求参数错误或业务校验失败 |
| 401  | 未登录或 Token 无效/过期  |
| 403  | 无权限（如账号被禁用）    |
| 500  | 服务器内部错误            |
